&load("pilude.pi");
out(stdout,"Enter Host"); in(stdin,host);
out(stdout,"Enter Port"); in(stdin,port);
    let ch = chan(host,port) in
        let proc = out_and_in(ch) in
            &forever(proc)
